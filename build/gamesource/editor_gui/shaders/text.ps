sampler2D txDiffuse : register(s0);

float4 g_color : register(c0);

struct PS_INPUT
{
    float4 Pos : POSITION;
	float2 Tex : TEXCOORD0;
};

float4 main(PS_INPUT input) : COLOR
{
	float4 finalColor = tex2D(txDiffuse, input.Tex);
	finalColor *= g_color;
    return(finalColor);
}