
#include <../struct.h>

sampler2D BaseSampler:register(s0);

float3 PlaneNormal;
float3 PlanePoint;

half4 main(vs_out_gcommon IN) : COLOR0
{
	clip(dot((IN.Pos.xyz / IN.Pos.w) - PlanePoint, PlaneNormal));
	return tex2D(BaseSampler,IN.TexUV);
}