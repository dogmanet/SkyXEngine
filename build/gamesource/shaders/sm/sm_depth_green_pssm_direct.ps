
#include <../struct.h>

sampler2D BaseSampler:register(s0);


float4 main(vs_out_green IN):COLOR0
{ 
	float4 tmpColor = tex2D(BaseSampler,IN.TexUV);
	clip(tmpColor.a - 0.5);
	tmpColor.a = 1.f;
	
	return IN.Pos.z/IN.Pos.w;
}