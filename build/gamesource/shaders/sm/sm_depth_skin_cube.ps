
/*
sm_depth_skin_cube.ps
Рендер глубины сцены (анимационные модели) с позиции точечного источника света
*/

#include <../struct.h>

sampler2D BaseSampler:register(s0);

half4 main(vs_out_gcommon IN) : COLOR0
{
	return length(IN.Pos);
}