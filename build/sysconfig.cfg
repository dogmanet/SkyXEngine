echo "Executing config file"

bind escape exit

bind w +forward
bind s +backward
bind a +left
bind d +right
bind shift +boost
bind ctrl +crouch
bind alt +crawl
bind space +jump
bind mouse1 +attack
bind mouse2 +attack2

cl_mode_editor 1
spawn

//cl_grab_cursor 0
